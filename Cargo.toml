[package]
name = "ao-server"
version = "0.1.0"
edition = "2024"
description = "Tierras Sagradas AO - Game Server (Rust port)"

[dependencies]
tokio = { version = "1", features = ["full"] }
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
thiserror = "2"
