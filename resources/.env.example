# Override the default compression key (optional).
# If not set, the CLI uses the built-in default key that matches the client binary.
# See resources/README.md for details.
AOPAK_KEY=your-custom-passphrase-here
